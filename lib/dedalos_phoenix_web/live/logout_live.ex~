defmodule DedalosPhoenixWeb.LogoutLive do

  use DedalosPhoenixWeb, :live_view
  
  def mount(_params, _sessions, socket) do
    {:ok, assign(socket, message: Hi, "auth_token": "")}
  end

  def render(assigns) do
    ~H"""
     <div>
     Hi 
     </div>
  
    """

    end

  end
